import React from "react";

const CartReducer = (state, action) => {
  switch (action.type) {
    case "UPDATE_CART":
      return state;
    default:
      return state;
  }
}

export default CartReducer;
